import { AdminPropertiesClient } from "@/features/properties/components";

export const metadata = {
  title: "Property Moderation — RealtorNet Admin",
};

export default function AdminPropertiesPage() {
  return <AdminPropertiesClient />;
}
